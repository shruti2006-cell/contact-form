# contact-form
The form includes client-side validation using JavaScript, ensuring that all fields are filled correctly before submission. Error messages are shown instantly for invalid inputs, and valid fields are marked with color changes.
